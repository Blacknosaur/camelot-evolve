# Android client

Planned native stack: Kotlin, Compose/Views where appropriate, Room/SQLite, WorkManager, CameraX, Media3, MediaCodec, and GPU effects. Keep the sync state machine independent from activities and media rendering.

